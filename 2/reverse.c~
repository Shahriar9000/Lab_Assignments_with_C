#include <stdio.h>
void reverse( int arr[], unsigned int len )
{	 
	int first=0;
	int last =len-1;
	int a;
	while (first < last)
	{ a= arr[first];
	  arr[first] = arr[last];
	  arr[last] = a;
	 first++;
	 last--;}
}



